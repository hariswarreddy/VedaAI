import { ComingSoon } from '@/components/ComingSoon';

export default function GroupsPage() {
  return (
    <ComingSoon
      title="My Groups"
      description="Organize your students into classes and sections, then push assignments to them in one tap."
    />
  );
}
